apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gateway-crds
  namespace: network-system
spec:
  interval: 5m
  chart:
    spec:
      chart: gateway-api
      sourceRef:
        kind: HelmRepository
        name: appscode-repo
        namespace: network-system
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
